// lib/features/home/side_quest/tilt_a_word_page.dart
import 'package:flutter/material.dart';

class TiltAWordPage extends StatelessWidget {
  const TiltAWordPage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('Tilt-A-Word')),
    body: const Center(child: Text('Coming next!')),
  );
}
